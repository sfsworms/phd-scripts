# Merging the datasets
 ## This function is used to merge datasets representing multiple repetitions of the same experiment.

library(tidyverse) # tidyverse
source(file = "C:/Users/worms/phd-scripts/tecan_growth/utils.R") # Load the functions for analysis of the tecan data
